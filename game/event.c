int onGameBlockChange(int x, int y, int blockType, int blockStatus);

int onGamePause(int status);

int onGameLineCrash(int line);

int onGameOver();

int onNetworkConnect();

int onNetworkDisconnect();
